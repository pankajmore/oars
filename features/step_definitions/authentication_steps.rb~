# Feature: Signing in
# 
#   Scenario: Unsuccessful signin                # ../../features/signing_in.feature:3
#     Given a user visits the signin page        # ../../features/signing_in.feature:4
#     When he submits invalid signin information # ../../features/signing_in.feature:5
#     Then he should see an error message        # ../../features/signing_in.feature:6
# 
#   Scenario: Successful signin                     # ../../features/signing_in.feature:8
#     Given a user visits the signin page           # ../../features/signing_in.feature:9
#     And the user has an account                   # ../../features/signing_in.feature:10
#     And the user submits valid signin information # ../../features/signing_in.feature:11
#     Then he should see his profile page           # ../../features/signing_in.feature:12
#     And he should see a signout link              # ../../features/signing_in.feature:13
# 
# 2 scenarios (2 undefined)
# 8 steps (8 undefined)
# 0m0.010s
# 
# You can implement step definitions for undefined steps with these snippets:

Given /^a user visits the signin page$/ do
  pending # express the regexp above with the code you wish you had
end

When /^he submits invalid signin information$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^he should see an error message$/ do
  pending # express the regexp above with the code you wish you had
end

Given /^the user has an account$/ do
  pending # express the regexp above with the code you wish you had
end

Given /^the user submits valid signin information$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^he should see his profile page$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^he should see a signout link$/ do
  pending # express the regexp above with the code you wish you had
end