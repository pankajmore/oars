<div class="center hero-unit">
        <h3>Current-Semester</h3>
        <h5>Here is your current Semester!</h5>

        <p>
        <div align="right" class="btn-group">
                <%= link_to "Pre Registration", student_pre_registration_path, class: "btn btn-large btn-success"  %>
        </div>
        <%if(!@currentRegistration.nil? ) %>
        <%if !@currentRegistration.course_doings.empty?%>
        <table class="center table">
                <tr class="info"><td>Course</td><td>Title</td><td>Units</td><td>Section</td><td>R/S</td><td>R/S Course </td></tr>
				
               <% @currentRegistration.course_doings.each do |course| %>      
                <tr><td><%=course.course.number%></td><td><%=course.course.name%></td><td><%=course.credits%></td><td><%=%>-</td><td><%=%>-</td><td><%=%>-</td></tr>
                 <% end %>
                
        </table>
        <% else %>
			you are not registered for any courses.
			
       	 <% end %>
         <% else %>
           Opps!! Your registration form for current semester is missing.
       	 <% end %>
       	 <br> your student id is <%=current_student.id%>
       	 </p>
        
</div>



<%= link_to image_tag("rails.png", alt: "Rails"), 'http://iitk.ac.in/' %>

