var body = "Instructors : <%= @course.faculties.map{|p| p.name}.join(', ') %> </br> Timings : <% @timings.each do |timer| %> day : <%= timer.day %>; time : <%= timer.starts_at.strftime('%H:%M:%S') %> - <%= timer.ends_at.strftime('%H:%M:%S') %></br> <% end %> Description: <%= @course.description %> </br> Pre Reqs: <% @course.course_prereqs.each do |pre| %> </br> <%= pre.number %> <%= pre.name %> <% end %> "
$(".modal-header h3").html("<%= @course.course.number + " : " + @course.course.name %>")
$(".modal-body").html(body)

$("#courseModal").modal()
