var body = "<% if @course.offered.nil? %> Not Offered this semester <% else %> Offered this semester <% end %>"
$(".modal-header h3").html("<%= @course.number + " : " + @course.name %>")
$(".modal-body").html(body)
$("#courseModal").modal()
