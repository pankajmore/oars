var body = "<% if @course.offered.nil? %> Not Offered next semester <% else %> Offered next semester <% end %>"
$(".modal-header h3").html("<%= @course.number + " : " + @course.name %>")
$(".modal-body").html(body)
$("#courseModal").modal()
